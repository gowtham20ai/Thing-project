# Root-Level TODO for Vercel Deploy Fix\n\n**Updated Diagnosis:** Vercel runs at repo root (no package.json), ignores shop-vite/ subdir. Added root vercel.json to fix.\n\n## Status: Fixed - Push to Deploy ✓\n\n### Changes:\n1. **Created `./vercel.json`**: `{"rootDirectory": "shop-vite"}` → Vercel cds to shop-vite/, runs npm ci + build there ✓\n2. shop-vite/vercel.json keeps Node 24.x, npm ci, dist output ✓\n3. Local shop-vite/ build confirmed success ✓\n\n### Deploy Steps:\n1. `git add . && git commit -m "fix(vercel): add root vercel.json for shop-vite subdir build" && git push`\n2. Vercel redeploys → success\n3. Preview URL ready.\n\n**Test Local Equivalent:** `cd shop-vite && npm ci && npm run build` (already ✓)
